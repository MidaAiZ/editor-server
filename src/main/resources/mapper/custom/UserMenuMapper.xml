<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mida.chromeext.dao.UserMenuDAO">
    <select id="listMenuByUserId" parameterType="integer" resultType="com.mida.chromeext.pojo.UserMenu">
        SELECT m.mid,
               m.title,
               m.url,
               m.icon_src,
               m.is_folder,
               m.folder_id,
               m.user_id,
               m.index,
               m.created_at
        FROM user_menus m
        WHERE m.user_id = #{userId}
    </select>

    <insert id="batchInsert" parameterType="map">
<<<<<<< HEAD
        insert into user_menus(title, url, icon_src, is_folder, folder_id, user_id, `index`, created_at)
=======
        insert into user_menus(title, url, icon_src, is_folder, folder_id, user_id, index, created_at)
>>>>>>> site search
        values
        <foreach collection="menuList" item="menu" separator="," index="index">
            <trim prefix="(" suffix=")">
                #{menu.title},
                #{menu.url},
                #{menu.iconSrc},
                #{menu.isFolder},
                #{menu.folderId},
                #{userId},
                #{menu.index},
<<<<<<< HEAD
                now()
=======
                unix_timestamp(now())
>>>>>>> site search
            </trim>
        </foreach>
    </insert>

    <delete id="batchDelete" parameterType="map">
        delete from user_menus
        where
        user_id = #{userId}
        and
        mid in
        <foreach collection="menuIdList" item="menuId" open="(" close=")" separator=",">
            #{menuId}
        </foreach>
    </delete>
</mapper>